# OpenOrcha

OpenOrcha is a local Express UI/API for launching, monitoring, and coordinating coding agents, with helper tabs for sessions, SSH hosts, keys, and raw config.

## Run

```bash
npm install
npm start
```

Then open `http://127.0.0.1:3456`.
